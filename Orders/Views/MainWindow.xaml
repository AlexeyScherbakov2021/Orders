<Window x:Class="Orders.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Orders.ViewModels"
        xmlns:local="clr-namespace:Orders"
        mc:Ignorable="d"
        Title="Список заказов" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500*"/>
            <ColumnDefinition Width="377*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="26.002"/>
        </Grid.RowDefinitions>
        <ToolBarPanel Grid.Row="0" Orientation="Horizontal" Grid.ColumnSpan="2">
            <Button Margin="5" Content="Создать" Command="{Binding CreateCommand}"/>
            <Button Margin="5" Content="Отправить" Command="{Binding }"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0">
                <RadioButton Content="Созданные" Margin="5,0" Command="{Binding FilterCommand}" CommandParameter="1"/>
                <RadioButton Content="На рассмотрение" Margin="5,0" Command="{Binding FilterCommand}" CommandParameter="2"/>
                <RadioButton Content="Закрытые" Margin="5,0" Command="{Binding FilterCommand}" CommandParameter="3"/>
                <RadioButton Content="Все" Margin="5,0" Command="{Binding FilterCommand}" CommandParameter="4"/>
            </StackPanel>
        </ToolBarPanel>
                <ListBox Margin="10,10,10,10" Grid.Row="1" x:Name="list" ItemsSource="{Binding ListOrders}" SelectedItem="{Binding SelectedOrder}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid >
                                <Grid.InputBindings>
                                    <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DataContext.DblClickCommand, ElementName=list}" />
                                </Grid.InputBindings>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding o_number}" />
                                <TextBlock Text="{Binding o_name}" Grid.Column="1"/>
                                <TextBlock Text="{Binding o_statusId}" Grid.Column="2"/>
                                <TextBlock Text="{Binding o_date_created}" Grid.Column="3"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
        <StatusBar Grid.Row="3">
            <StatusBarItem Content="Пользователь:  "/>
            <StatusBarItem Content="{Binding CurrentUser.u_name}"/>
        </StatusBar>
    </Grid>
</Window>
