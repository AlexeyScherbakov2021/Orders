<Window x:Class="Orders.Views.OrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Orders.ViewModels"
        xmlns:local="clr-namespace:Orders.Views"
        mc:Ignorable="d"
        Title="Заказ" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:OrderWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="56,38,0,0" Text="Номер" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="56,58,0,0" Text="Наименование" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="56,78,0,0" Text="Статус" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="56,98,0,0" Text="Текст" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="56,118,0,0" Text="Время создания" VerticalAlignment="Top"/>

        <TextBlock HorizontalAlignment="Left" Margin="206,38,0,0" Text="{Binding order.o_number}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="206,58,0,0" Text="{Binding order.o_name}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="206,78,0,0" Text="{Binding order.RouteStatus.sr_name}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="206,98,0,0" Text="{Binding order.o_body}" VerticalAlignment="Top"/>
        <TextBlock HorizontalAlignment="Left" Margin="206,118,0,0" Text="{Binding order.o_date_created}" VerticalAlignment="Top"/>

        <TextBox Text="Добавить сопроводительный текст" Margin="56,158,454,341" />
        <TextBlock HorizontalAlignment="Left" Margin="56,260,0,0" TextWrapping="Wrap" Text="Прикрепить файлы" VerticalAlignment="Top"/>
        <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding CurrentStep.RouteAddings}" Width="290" Height="190" Margin="56,284,0,0" BorderBrush="Black" BorderThickness="1,1,1,1">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ad_text}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock HorizontalAlignment="Left" Margin="517,45,0,0" Text="Маршрут заказа" VerticalAlignment="Top"/>

        <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding order.RouteOrders}" 
                      Width="325" Height="396" Margin="449,78,0,0" BorderBrush="Black" BorderThickness="1,1,1,1">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="30"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding ro_step}" Grid.Column="0"/>
                        <TextBlock Text="{Binding User.u_name}" Grid.Column="1"/>
                        <TextBlock Text="{Binding RouteType.rt_name}" Grid.Column="2"/>
                        <CheckBox IsChecked="{Binding ro_check}" Grid.Column="3"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
